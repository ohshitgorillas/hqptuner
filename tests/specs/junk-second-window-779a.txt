slug: junk-second-window-779a
kind: new
brief:
> Replace the junk detector's 30 s block window (metering.py: BLOCK_SECONDS, WINDOW_BLOCKS, the block deque and window_min_db()) with a per-bin minimum over the most recent ingested hops whose summed coverage (xform_time * DECIMATE) reaches WINDOW_SECONDS = 1.0, oldest hop dropped as newer ones arrive, silent hops still held out; the window is specified in seconds because hop length follows source rate. All three rules (20k, spur, 50k) read that 1 s minimum curve with no threshold change, auto-pilot follows the resulting verdict directly with no debounce on the write path, and the junkcal capture stores that curve in place of the 30 s minimum. This supersedes the approved plan junk-instant-spectrum (not implemented) and amends the rulings at docs/gauntlet/plans/approved/junk-live-verdict.txt:12 and :14 to permit this one window and nothing else; state the amended wording, what each rule now costs in false firing and drop-out against a 1 s minimum, and the time to first verdict.

1. Sweep on how much coverage one aggregate has been fed, every hop non-silent, carrying the same power in one bin, read once through `window_min_db()`: four hops covering 0.05 s apiece yields None, twenty hops covering 0.04 s apiece yields None, four hops covering 0.24 s apiece yields None, two hops covering 0.5 s apiece yields that bin at -20.0 dB, four hops covering 0.25 s apiece yields that bin at -20.0 dB.
   kills: a readiness gate counting ingested hops instead of summing their coverage, which answers the four-hop 0.05 s case and the four-hop 0.25 s case alike
   bite: HEAD returns None at all five cases, where the line says the bin at -20.0 dB at the last two. 1a None, frames 4, seconds 0.2; 1b None, frames 20, seconds 0.8; 1c None, frames 4, seconds 0.96; 1d None, frames 2, seconds 1.0; 1e None, frames 4, seconds 1.0. Control on the same constructor: 0.5 s hops first return non-None at frames 60 / seconds 30.0, bin 2 at -20.0, so the entry point does produce values and every case here is below the coverage HEAD's window requires. (`PYTHONPATH=. .venv/bin/python -c "from hqptuner.engine.metering import SpectralAggregate
m=[10**-12.0]*2+[10**-2.0]+[10**-12.0]*5
for tag,n,cov in [('1a',4,0.05),('1b',20,0.04),('1c',4,0.24),('1d',2,0.5),('1e',4,0.25)]:
    a=SpectralAggregate(8,48000.0)
    for _ in range(n): a.add(list(m),cov)
    w=a.window_min_db()
    print(tag, w if w is None else round(w[2],6), a.frames, round(a.seconds,6))"`)
   existing: none. `grep -rn "SpectralAggregate\|window_min_db\|silent=True" tests/` returns tests/engine/test_junk_persistence.py:4, :5, :11, :25, :27, :51, :54, :58, :61, :64, :67, :75, :79, :82, :85, :86, :88, :101, :104, :105 and committed spec text under tests/specs/ that holds no test. Two of those tests pin the readiness gate this line restates at a different duration and go with it: ::test_window_min_is_none_before_the_window_is_earned at :50, whose body is five hops of BLOCK_SECONDS asserting None, and ::test_window_min_appears_once_the_window_is_earned at :58, whose body is six hops of BLOCK_SECONDS asserting the constant bin. `grep -rn "WINDOW_BLOCKS\|BLOCK_SECONDS" tests/` returns tests/engine/test_junk_persistence.py:25, :52, :53, :59, :60, :68, :69, :70, :102, :103, :104 only, so the import at :25 moves with them.

2. One aggregate fed hops of 0.5 s coverage apiece, all non-silent, one bin carrying -90.0 dB in the first two hops, then -20.0 dB in every hop after them, read twice through `window_min_db()`: taken after the third hop that bin reads -90.0 dB; taken again after the fourth hop it reads -20.0 dB.
   kills: an aggregate whose minimum folds in every hop it has ever ingested and never drops the oldest, which reads -90.0 dB at both readings
   bite: HEAD is None at both readings, where the line says -90.0 dB then -20.0 dB. Reading A None, frames 3, seconds 1.5; reading B None, frames 4, seconds 2.0. (`PYTHONPATH=. .venv/bin/python -c "from hqptuner.engine.metering import SpectralAggregate
def m(db): return [10**-12.0]*2+[10**(db/10)]+[10**-12.0]*5
a=SpectralAggregate(8,48000.0)
for i in range(4):
    a.add(m(-90.0 if i<2 else -20.0),0.5)
    if i==2:
        w=a.window_min_db(); print('2A', w if w is None else round(w[2],6), a.frames, round(a.seconds,6))
w=a.window_min_db(); print('2B', w if w is None else round(w[2],6), a.frames, round(a.seconds,6))"`)
   existing: none for a bin whose level changes as older hops age out. `grep -rn "_VARYING_BIN\|_CONSTANT_BIN" tests/` returns tests/engine/test_junk_persistence.py:30, :31, :38, :39, :61, :75, :79, :105. The nearest case, ::test_an_intermittent_bin_reports_its_low_level at :78, reads the low level of a bin fed high and low alternately and never reads it back high, so it pins the opposite of this line's second reading under the shorter window and goes with the helper `_earned_alternating` at :64 that feeds it thirteen hops of BLOCK_SECONDS.

3. One aggregate fed four hops of 0.5 s coverage apiece, in order: one non-silent hop carrying -90.0 dB in one bin, two hops of digital silence at the floor on every bin, one non-silent hop carrying -20.0 dB in that bin, read twice through `window_min_db()`: taken after the third hop it is None; taken again after the fourth hop that bin reads -90.0 dB.
   kills: an aggregate that counts a silent hop's coverage toward the window while keeping it out of the minimum, which has aged the -90.0 dB hop out by the second reading and returns -20.0 dB there
   bite: HEAD is None at both readings, where the line says -90.0 dB at the second. Reading A None, frames 3, seconds 1.5; reading B None, frames 4, seconds 2.0. (`PYTHONPATH=. .venv/bin/python` on a throwaway script constructing `SpectralAggregate(8, 48000.0)`, adding the four hops in order and printing `window_min_db()[2]`, `frames` and `seconds` after hops 3 and 4; total coverage 2.0 s never reaches one `BLOCK_SECONDS` block, so `window_min_db()` returns None before `WINDOW_BLOCKS` full blocks exist)
   existing: none for a silent hop's coverage being held out of the window. `grep -rn "silent" tests/` returns tests/engine/test_junk_persistence.py:10, :11, :44, :82, :84, :85, :88, :92, :96, :100, :101, :104, plus tests/support/fake_metering.py and tests/engine/test_metering_idle.py wire sites carrying `rms`. ::test_silent_frames_never_lower_the_window_min at :100 pins the other half of this line, that silence does not lower the minimum, at seven hops of BLOCK_SECONDS apiece, and goes with them; ::test_silent_frames_count_toward_frames at :92 and ::test_silent_frames_count_toward_seconds at :96 pin `frames` and `seconds`, which this change does not touch, and stay.

4. One running reader on a stream whose every wire frame carries a content ceiling at 22.5 kHz in a 96 kHz container, each frame declaring a transform time of 0.2 s, read twice through `recommendation()`: taken once the reader has ingested one hop it is None; taken once it has ingested two hops it names the 20k filter.
   kills: a window summing each hop's declared transform time without the DECIMATE factor the reader ingests at, which is still None at the second reading, where 1.6 s of covered playback has arrived
   bite: HEAD is None at both readings, where the line says the 20k filter at the second. Reading A `recommendation()` None, `aggregate().frames` 1, `aggregate().seconds` 0.800000011920929, `window_min_db()` None; reading B `recommendation()` None, frames 2, seconds 1.600000023841858, `window_min_db()` None. Four wire frames reach one ingested hop, eight reach two, DECIMATE being 4. (`PYTHONPATH=/home/atom/dev/hqptuner /home/atom/dev/hqptuner/.venv/bin/python <throwaway>.py`, deleted in the same command, which put tests/ and tests/support on sys.path, built `frame(cutoff_96k(22500.0), 48000.0, 0.2)`, and drove `conftest.running_reader` at `PLAYING` over a `MeteringStream`, waiting with `stream.flushed()` plus `eventually(lambda: reader.aggregate().frames >= N)` as tests/engine/test_junk_persistence.py:133-168 does)
   existing: tests/engine/test_junk_persistence.py::test_no_verdict_until_the_window_is_covered at :160 drives this same shape against the 30 s window, thirty wire frames of `CUT_22K5_FRAME` read at seven ingested hops asserting None, then thirty more read at fifteen asserting the 20k filter, so this line is that test reparametrized onto a shorter frame and it travels as a delta on it. `grep -rn "recommendation()" tests/` returns 28 sites: tests/audit/test_autopilot_audit.py:313, tests/live/test_autopilot.py:185, tests/engine/test_junk_advisor.py:168, :169, :186, :198, :224, :231, :233, :234, :241, tests/engine/test_metering_idle.py:68, :181, :194, :207, and tests/engine/test_junk_persistence.py:142, :154, :157, :166, :169, :178, :179, :188, :189, :200, :201, :225, :226. All but two enter through a batch of `FAKE_HIRES_FRAME` or `CUT_22K5_FRAME` at 0.7 s per wire frame, which is 2.8 s of coverage per ingested hop, so they earn a one-second window on their first hop and keep their readings. The two that do not are this line's delta at :160 and tests/engine/test_junk_advisor.py::test_coverage_is_measured_in_seconds_not_frames at :189, which streams sixty wire frames of 0.05 s, fifteen ingested hops covering 3.0 s, and asserts None; that expectation inverts under a one-second window, so it travels as a delta too. One more on the same surface takes a delta: tests/engine/test_junk_persistence.py::test_a_verdict_follows_the_window_and_does_not_outlive_it at :146, whose loop sends `CUT_22K5_FRAME` and `FLAT_FULLBAND_FRAME` in alternating groups of DECIMATE, so the twelfth ingested hop it reads `earned` at is a full-band one; under a one-second window that reading is the last hop alone, carries no ceiling, and `earned["filter"]` at :157 raises instead of reading "20k".

(The block as it reached the reviewer carried `kind:`, the brief and these behavior lines only: no public-entry-point, wire-fact, fixture or changelog section arrived.)

--- spec-reviewer READY ---
READY
discriminates: sweep on SpectralAggregate.window_min_db() readiness by summed coverage (line 1), with differentials on eviction (line 2), on silent coverage held out of the window (line 3) and on the wire verdict (line 4)

1  DELTA tests/engine/test_junk_persistence.py:50 (carried, round 1): ::test_window_min_is_none_before_the_window_is_earned (:50) and ::test_window_min_appears_once_the_window_is_earned (:57) are this readiness gate on the same surface with the same two assertions; the line is those two tests reparametrized onto summed coverage with three more cases, so it folds into them rather than landing as a new test.
2  KEEP  (carried, round 1) four hops of 0.5 s coverage, one bin -90.0 dB for the first two then -20.0 dB, read after hop 3 and after hop 4 -> -90.0 dB then -20.0 dB; an aggregate folding in every hop ever ingested fails it at the fourth-hop reading, returning -90.0 dB; bite: HEAD None at both readings (frames 3/seconds 1.5 and frames 4/seconds 2.0), measured with the pasted constructor command
3  KEEP  one non-silent hop at -90.0 dB in a bin, two silent 0.5 s hops, one non-silent hop at -20.0 dB in that bin, read after hop 3 and after hop 4 -> None then -90.0 dB; an aggregate counting silent coverage toward the window fails it at the fourth-hop reading, having aged the -90.0 dB hop out and returning -20.0 dB; bite: HEAD None at both readings (frames 3/seconds 1.5 and frames 4/seconds 2.0), so the second reading bites
4  DELTA tests/engine/test_junk_persistence.py:160: ::test_no_verdict_until_the_window_is_covered drives this shape at 0.7 s wire frames against the 30 s window; the line is that test reparametrized onto 0.2 s frames, read at one and at two ingested hops. Two further deltas the line's `existing:` names travel with it and are approved as part of it: tests/engine/test_junk_advisor.py:189 ::test_coverage_is_measured_in_seconds_not_frames, whose 3.0 s of coverage asserts None and inverts under a one-second window, and tests/engine/test_junk_persistence.py:146 ::test_a_verdict_follows_the_window_and_does_not_outlive_it, whose twelfth-hop reading at :157 is a full-band hop carrying no ceiling once the window is the last hop alone.

null stub: SpectralAggregate.add() records nothing, window_min_db() returns None at every call, MeteringReader.recommendation() returns None at every call.
  satisfies: none   fails: 1, 2, 3, 4
  reading on 3: reading A's None agrees by accident, reading B's -90.0 dB does not, so the line as a whole fails it.
hard-coded stub: ignore coverage and per-hop levels; count non-silent hops ingested, return None below two and a canned curve carrying -20.0 dB in bin 2 at or above two; recommendation() None below two ingested hops, then the canned 20k dict.
  satisfies: 4   fails: 1, 2, 3
  reading on 1: four hops at 0.05 s would return the curve where the line says None, and two hops at 0.5 s would return None where the line says -20.0 dB.
  reading on 2: returns -20.0 dB at the third-hop reading where the line says -90.0 dB.
  reading on 3: returns the canned -20.0 dB at the fourth-hop reading where the line says -90.0 dB; that expected value is the level of a hop three back, which only a window excluding silent coverage still holds, so no canned curve keyed on hop counts reaches it. Round 1's line 3 died here and this one does not.
  reading on 4: satisfied - every frame the line feeds carries the ceiling, so the stub never has to look at a spectrum. The line is a DELTA and writes no new test: it reparametrizes :160, whose file-mates at :146, :157 and :172 kill that stub, and the delta is compulsory because :160's present assertion is written against the window this change deletes.

survives: 2

notes:
- Whether the junkcal capture module exists at HEAD, and whether it reads window_min_db() as a pass-through, is inside the implementation tree and barred to me. The brief's sentence "the junkcal capture stores that curve in place of the 30 s minimum" is pinned by no line in this block. The author's grep is confirmed independently: no test anywhere under tests/ names junkcal, and tests/specs/junkcal-a4a6.txt is committed spec text holding no test, so no delta target exists and a new line would be the only route. I do not hold the gate on it, since whether such a line has any code to pin is a fact I cannot read. Owner's call.
- The block as it reached me carried `kind:`, the brief and the behavior lines only; no public-entry-point, wire-fact, fixture or changelog section arrived. The approved spec file carries exactly what was reviewed.
