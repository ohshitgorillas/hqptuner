slug: junk-live-verdict-9045
kind: new
brief:
> Rewrite the junk-filter detector's 20k and 50k rules to read the windowed minimum spectrum (`window_min_db()`), as the spur rule already does. Both rules currently read a mean spectrum accumulated over time; that accumulator goes, and so does the verdict latch: a verdict is a property of the current spectrum, recomputed every tick, held by nothing. The "fake lossy hi-res" case goes with it: a source that brick-walls below 20 kHz gets no verdict, because the 20k filter does nothing to it.
> No time-averaging and no memory of past verdicts survives anywhere in the detection path.
> A filter is recommended, and auto-pilot engages it, only when it has an effect on what is playing. 20k needs a content ceiling above 20 kHz with near-floor energy above it. 50k needs a container whose bandwidth reaches past the corner; derive the rate floor from that and state it.
> Auto-pilot follows the live verdict directly: engage when present, release when absent. No hysteresis, no hold timer.
> MY BRIEF IS EXPLICIT: EXCISE ALL TESTS THAT NO LONGER PIN A VALID CONTRACT. I AM NOT CONCERNED WITH HOW WELL-FORMED THEY ARE.
> Your block must state both post-change signatures and carry an existing: clause for every test file the plan names — tests/engine/test_junk_advisor.py, tests/engine/test_junk_persistence.py, tests/engine/test_metering_idle.py, tests/live/test_autopilot.py, tests/support/junk_spectra.py — including tests/live/test_autopilot.py:66 and :70, which call classify positionally and therefore match no grep of the deleted symbols.
> The owner has ruled that no CHANGELOG.md entry is written until the feature is finished including the spur rule, so the block says "no entry" and CHANGELOG.md:13 stays untouched.

1. One running reader, one stream, two readings of `recommendation()`: taken after a full persistence window whose every block minimum carries a content ceiling at 22.5 kHz, while the mean over the same ingested frames carries none, the last ingested frame being full-band, it names the 20k filter; taken again after a further full persistence window of full-band frames carrying no ceiling, it is None.
   kills: a reader that accumulates its evidence and keeps its answer, reading a power mean since playback started and holding a verdict once earned, which is None at reading A where the mean carries no ceiling; a reader that reads the windowed minimum but keeps its answer, which names the 20k filter at reading B; and a memoryless reader classifying the newest frame alone, which is None at reading A, whose last ingested frame is full-band
   bite: HEAD is None at reading A, where the line says the 20k filter. Reading A `None`, `aggregate().seconds` 33.59999942779541, `aggregate().frames` 12, ingested split 6 CUT / 6 full-band, last ingested frame full-band, `window_min_db()[700]` at 32812.5 Hz -106.9897 and `[200]` at 9375.0 Hz -16.9897, so the minimum carries the 22.5 kHz ceiling at that reading; reading B `None`, `aggregate().seconds` 67.19999885559082, `aggregate().frames` 24, cumulative split 6 CUT / 18 full-band, last ingested frame full-band, `window_min_db()[700]` at 32812.5 Hz -16.9897. Command: a throwaway script run as `PYTHONPATH=/home/atom/dev/hqptuner /home/atom/dev/hqptuner/.venv/bin/python $SCRATCH/measure.py`, deleted in the same command, which builds `MeteringStream` as `fixtures_daemons.metering_stream` does, drives it through `conftest.running_reader(port, [PLAYING])` with an instant sleep seam, sends `CUT = frame(cutoff_96k(22500.0), 48000.0, 0.7)` and `FULL = frame(flat_fullband_96k(), 48000.0, 0.7)` as `stream.send(CUT, count=DECIMATE)` then `stream.send(FULL, count=DECIMATE)` six times over (48 wire frames, 12 ingested at 2.8 s apiece, each pushed block one CUT plus one full-band frame), waits on `stream.flushed()` then polls `aggregate().frames` to a 0.30 s plateau, prints `repr(reader.recommendation())`, then sends 48 further full-band wire frames and prints again. Levels read 3.01 dB above the nominal -20 / -110 dB because `fake_metering.frame` writes the level into both channels and `_frame_power` sums channels
   existing: tests/engine/test_junk_persistence.py::test_a_verdict_latches_for_the_rest_of_the_track at :138, which drives this same earn-then-erase input and pins the opposite second reading: 30 fake-hi-res frames then 60 full-band frames, asserting the 20k recommendation stands. `grep -rn "_latched_20k\|_earn_20k" tests/` returns the helper at tests/engine/test_junk_persistence.py:131 (`stream.send(FAKE_HIRES_FRAME, count=30)`) with callers at :142, :152, :162, :174 and :186, and a second helper at tests/engine/test_metering_idle.py:65 with callers at :176, :191 and :204, all of which enter this surface through a batch shorter than the persistence window. `grep -rn "levels_db()" tests/` returns tests/engine/test_junk_persistence.py:103 alone, test_silent_frames_still_weigh_into_the_mean, the suite's one reader of the removed mean. The full-band frame this line's reading B streams is built from tests/support/junk_spectra.py:49 `flat_fullband_96k`, whose docstring at :52-53 states the opposite contract ("Mixed into brick-wall evidence it erases the cliff from the cumulative mean, so no re-classification can recommend"), and is wired into a frame at tests/engine/test_junk_persistence.py:121. Two tests on this surface pin the contract this line reverses and pin nothing valid after it, so both go: tests/engine/test_junk_persistence.py:101-103 test_silent_frames_still_weigh_into_the_mean, the suite's one reader of the deleted mean, and :180-191 test_disengaging_brings_the_latched_verdict_back_without_new_frames, whose "without new frames" is the held answer this line removes.

2. Sweep on the content ceiling of a 96 kHz container, one spectrum flat to the cutoff, at the floor above it, handed to `classify(min_levels_db, bandwidth, *, samplerate, sdm)` as its single spectrum: 17.0 kHz yields None, 20.0 kHz yields None, 22.5 kHz yields the 20k filter reporting a ceiling of 22.5 kHz.
   kills: a rule recommending the 20k corner for a ceiling the corner cannot act on, such as a lossy encode cutting at 17 kHz inside a hi-res container
   bite: HEAD returns filter '20k' at all three cutoffs, ceiling_khz 17.0, 20.0 and 22.5 — `{'filter': '20k', 'reason': 'Content stops at 17.0 kHz in a 96 kHz container — consistent with fake hi-res. Recommend engaging the 20k high-frequency filter.', 'ceiling_khz': 17.0}` and the same shape at 20.0 and 22.5. Command: `.venv/bin/python - <<'PY'` with `sys.path[:0] = ["/home/atom/dev/hqptuner", "/home/atom/dev/hqptuner/tests/support"]`, `from junk_spectra import cutoff_96k`, `from hqptuner.engine.junkadvisor import classify`, `for hz in (17000.0, 20000.0, 22500.0): lv = cutoff_96k(hz); print(hz, classify(lv, 48000.0, 60.0, samplerate=96000, sdm=False, min_levels_db=lv))` `PY`
   existing: tests/engine/test_junk_advisor.py::test_fake_hires_reports_the_content_ceiling, whose parametrize case `(cutoff_96k(14000.0), "20k", 14.0)` at :112 pins the opposite for a ceiling below 20 kHz, and whose `constant_fall_96k(18.0)` case at :109 pins a 19.6 kHz ceiling as "20k". `grep -rn "ceiling_khz" tests/` returns that test at :107-121, the spur ceiling at :152, and no other Python pin on this field. Two more sites drive this surface through the argument list this line changes: `grep -rn "MIN_SECONDS" tests/` returns tests/engine/test_junk_advisor.py:39 (the import) and :77 (test_no_verdict_below_the_minimum_coverage, passing `seconds=MIN_SECONDS - 0.1`), and the `_classify` helper at :43-59 passes `seconds` on behalf of every advisor case in that file. The cutoff builders this line sweeps are tests/support/junk_spectra.py:26 `constant_fall_96k` and :38 `cutoff_96k`, imported at tests/engine/test_junk_advisor.py:26-27. Two more tests enter through the argument list this line's call form drops, with their dispositions: tests/engine/test_junk_advisor.py:76-77 test_no_verdict_below_the_minimum_coverage passes `seconds=MIN_SECONDS - 0.1` on a symbol this change deletes, pins nothing valid afterwards, so it goes, its readiness contract passing to line 4; tests/engine/test_junk_advisor.py:129-131 test_hf_tone_in_the_mean_alone_earns_no_spur_verdict passes `min_levels_db=None` beside a mean the single-spectrum call has no room for, so it travels as a DELTA to `classify(None, 88200.0, samplerate=176400, sdm=False) is None`.

3. Sweep on container bandwidth for one rising-noise shape, handed to the same single-spectrum `classify(min_levels_db, bandwidth, *, samplerate, sdm)`: at 48 kHz bandwidth the verdict is None, at 88.2 kHz bandwidth it is the 50k filter.
   kills: a rule recommending the 50k corner in a container whose bandwidth stops below the corner, where the filter removes nothing from what is playing
   bite: HEAD returns filter '50k' for both — `{'filter': '50k', 'reason': 'HF noise rising toward 48 kHz — consistent with excessive noise shaping (some ADCs, DSD-to-PCM transfers). Recommend engaging the 50k high-frequency filter.', 'ceiling_khz': 48.0}` at 48 kHz bandwidth and the same shape with ceiling_khz 88.2 at 88.2 kHz. Command: `.venv/bin/python - <<'PY'` with `sys.path[:0] = ["/home/atom/dev/hqptuner", "/home/atom/dev/hqptuner/tests/support"]`, `from junk_spectra import shaping_ramp_176, spectrum`, `a = shaping_ramp_176(); print(classify(a, 88200.0, 60.0, samplerate=176400, sdm=False, min_levels_db=a))`, `def level(f): return -20.0 - 90.0 * f / 20000.0 if f <= 20000.0 else (-110.0 if f <= 24000.0 else -110.0 + 50.0 * (f - 24000.0) / (48000.0 - 24000.0))`, `b = spectrum(48000.0, level); print(classify(b, 48000.0, 60.0, samplerate=96000, sdm=False, min_levels_db=b))` `PY`
   existing: tests/engine/test_junk_advisor.py::test_noise_shaping_ramp_recommends_the_50k_filter at :165-166 pins the 88.2 kHz half only. `grep -rn "50k" tests/` returns that test, the treats case at tests/engine/test_junk_advisor.py:376, the families case at :339 reading the same shape, and tests/live/test_autopilot.py:133 (`junk_filter_index(ITEMS, "50k") is None`, an enumeration lookup carrying no verdict), none of them covering a 48 kHz-bandwidth container. The shape this line sweeps is tests/support/junk_spectra.py:73 `shaping_ramp_176`, whose docstring states the 176.4 kHz container it was built for.

4. One running reader on a stream whose every frame carries a content ceiling at 22.5 kHz, read twice: at 19.6 s of counted coverage `recommendation()` is None; at 42.0 s it names the 20k filter. `eligible(samplerate, bandwidth, bins, *, sdm)` carries no coverage argument, so the wait is the persistence window.
   kills: a detector answering from coverage shorter than the persistence window, which names the 20k filter at 19.6 s on evidence the windowed minimum has not yet earned
   bite: HEAD names the 20k filter at both readings, where the line says None at the first. Reading A `{'filter': '20k', 'reason': 'Content stops at 22.5 kHz in a 96 kHz container — consistent with fake hi-res. Recommend engaging the 20k high-frequency filter.', 'ceiling_khz': 22.5}`, `aggregate().seconds` 19.59999966621399, `aggregate().frames` 7, with `window_min_db()` None there on 3 pushed blocks; reading B the same dict character for character, `aggregate().seconds` 41.99999928474426, `aggregate().frames` 15, window earned. Command: the same throwaway script run as `PYTHONPATH=/home/atom/dev/hqptuner /home/atom/dev/hqptuner/.venv/bin/python $SCRATCH/measure.py`, deleted in the same command, which sends `CUT = frame(cutoff_96k(22500.0), 48000.0, 0.7)` as `stream.send(CUT, count=30)`, drains on `stream.flushed()` and polls `aggregate().frames` to a 0.30 s plateau, prints `repr(reader.recommendation())`, then sends `stream.send(CUT, count=30)` again and prints again. 30 wire frames yield 7 ingested at `xform_time * DECIMATE` = 2.8 s each, so 19.6 s counted rather than the 21 s the existing helpers' comments claim, and the decimation counter runs on across the second batch for 15 ingested and 42.0 s at reading B
   existing: tests/engine/test_junk_persistence.py::test_track_change_clears_the_latched_verdict at :148, ::test_stream_loss_clears_the_latched_verdict at :158 and ::test_engaging_the_recommended_filter_quiets_the_latched_verdict at :168 pin contracts this change keeps — evidence ownership on a serial change, stream loss, and the engaged filter treating the signature — and go red only on the coverage this line states, so they travel as DELTAs on it with the shared helper at :131 earning a full window. tests/engine/test_metering_idle.py::test_the_idle_close_keeps_the_verdict_earned_before_the_pause at :169, ::test_a_stream_that_ends_while_playing_discards_the_verdict at :184 and ::test_a_stream_that_errors_while_playing_discards_the_verdict at :197 enter through `_earn_20k` at :65, whose `stream.send(FAKE_HIRES_FRAME, count=30)` is the same short batch and takes the same delta. tests/live/test_autopilot.py:66 and :70 build the module constants BRICKWALL and SPUR by calling `classify` positionally — `classify(fake_hires_96k(), 48000.0, 60.0, samplerate=96000, sdm=False)` and `classify(decaying_176(), 88200.0, 60.0, samplerate=176400, sdm=False, min_levels_db=spur_min_176(40000.0))` — so both match no grep of a deleted symbol and both stop resolving at the new signature; `grep -rn "classify(" tests/` returns those two sites plus tests/engine/test_junk_advisor.py:43-70 (the `_classify` and `_classify_spur` helpers) and its 14 call sites at :77-380. The frame these readings stream is tests/support/junk_spectra.py:87 `FAKE_HIRES_FRAME`, built from `fake_hires_96k()` at :21 with 0.7 s of coverage per frame. Both autopilot constants travel as a plumbing DELTA on the single-spectrum call, the same two verdicts rebuilt as `classify(fake_hires_96k(), 48000.0, samplerate=96000, sdm=False)` at :66 and `classify(spur_min_176(40000.0), 88200.0, samplerate=176400, sdm=False)` at :70, the mean curve `decaying_176()` dropping out of the second with the argument it fed.

5. One `advising` manager on the fake control daemon with auto-pilot enabled, its verdict earned from the fixture's covering batch of fake-hi-res frames, read twice through `junk_writes(log)`: after one `act(manager)` while the verdict stands it is `["1"]`; after a further full persistence window of full-band frames, a second `act(manager)` makes it `["1", "0"]`.
   kills: an auto-pilot that engages on a verdict, then holds the corner engaged for the rest of playback once the signature it was engaged for has left the spectrum, releasing only on a change of what is playing
   bite: HEAD is `['1']` at the second reading, where the line says `["1", "0"]`. Reading 1 `junk_writes(log)` `['1']`, `aggregate().seconds` 41.99999928474426, `aggregate().frames` 15 (15 fake-hi-res ingested, 0 full-band); reading 2 `junk_writes(log)` `['1']`, `aggregate().seconds` 83.99999856948853, `aggregate().frames` 30 (15 fake-hi-res, 15 full-band), from 60 full-band wire frames sent, 15 ingested at `DECIMATE` = 4 and 2.8 s apiece, which is one and a quarter persistence windows (`BLOCK_SECONDS` 5.0, `WINDOW_BLOCKS` 6). `manager.metering.verdict()` at reading 2 is `{'filter': '20k', 'reason': 'Content stops at 22.0 kHz in a 96 kHz container — consistent with fake hi-res. Recommend engaging the 20k high-frequency filter.', 'ceiling_khz': 22.0}`, still standing on HEAD; `recommendation()` reads None after either `act` because the corner `act` engaged treats the signature, which is why this line's observable is the command log rather than the note. Command: `timeout 120 nice .venv/bin/python $SCRATCH/measure5.py`, run from /home/atom/dev/hqptuner and deleted in the same command, a throwaway replicating the `advising` fixture of tests/live/test_autopilot.py:141-199 verbatim, calling `manager.presetops.autopilot.enable()` then `act(manager)`, printing `junk_writes(log)`, then sending 60 full-band frames built as `frame(flat_fullband_96k(), 48000.0, 0.7)`, draining on `stream.flushed()`, calling `act(manager)` again and printing `junk_writes(log)`
   existing: none for release after a verdict was earned. `grep -rn "junk_writes(log)" tests/` returns eight sites, all in tests/live/test_autopilot.py: :229, :241, :254, :262, :268, :277, :287, :297. `grep -rn "advising(frames=0" tests/` returns tests/live/test_autopilot.py:216, :237, :250, :258, which are every release-shaped case in the file — the State-read release at :213-220, the command-log release at :232-241, the rate-relative releases at :244-254 and the resting case at :257-262 — so no verdict is ever earned in any of them, and a reader that keeps its answer passes all four. The engage half is pinned at :223-229 (`junk_writes(log) == ["1"]`), which this line's first reading repeats as its anchor. The fifth line earns its place because lines 1 to 4 stop at the detector's own answer, while the brief's release ruling is about the write path, whose every existing release case starts from a manager that never had a verdict to release.

changelog: no entry — the owner has ruled that no CHANGELOG.md entry is written until the feature is finished including the spur rule; CHANGELOG.md:13 stays untouched.

note on sections: the block as submitted to review carried the `kind:` line, the `brief:` section and these behavior lines; no separate public-entry-points, wire-facts or fixtures sections were submitted, and none is invented here. The post-change signatures the brief required are stated inside the lines: `classify(min_levels_db, bandwidth, *, samplerate, sdm)` (lines 2, 3) and `eligible(samplerate, bandwidth, bins, *, sdm)` (line 4).

--- spec-reviewer READY ---
READY
discriminates: sweep on classify(min_levels_db, bandwidth, ...) at three content ceilings (line 2), plus differentials on recommendation() (lines 1 and 4) and on junk_writes(log) (line 5)

1  DELTA tests/engine/test_junk_persistence.py:138 (carried): same earn-then-erase input, second reading flips from "20k" to None, each phase a full persistence window; the helper at :131 changes with it, and :101-103 and :180-191 go with the mean and the latch they pin.
2  DELTA tests/engine/test_junk_advisor.py:106 (carried): same parametrize on the content ceiling - 14.0 and 19.6 kHz flip to None, 17.0/20.0/22.5 kHz become its cases; :76-77 goes with MIN_SECONDS and :129-131 takes the single-spectrum call.
3  DELTA tests/engine/test_junk_advisor.py:165 (carried): the single 88.2 kHz case becomes a two-case parametrize on container bandwidth, 48 kHz yielding None.
4  KEEP  (carried) 30 wire frames of cutoff_96k(22500) then 30 more -> recommendation() is None at 19.6 s of counted coverage and names the 20k filter at 42.0 s; a detector answering from coverage shorter than the persistence window names 20k at 19.6 s; bite: HEAD names the 20k filter at both readings (ceiling_khz 22.5), measured.
5  KEEP  one `advising` manager, auto-pilot enabled, covering batch then 60 full-band wire frames -> junk_writes(log) is ["1"] after the first act and ["1", "0"] after the second; an auto-pilot holding the corner once the signature has left the spectrum leaves it ["1"] at the second reading; bite: HEAD is ['1'] at the second reading, measured, with the verdict still standing there. Cap sentence accepted: lines 1-4 stop at the detector's answer, and every existing release case (:216, :237, :250, :258) starts from a manager that never earned a verdict, so a keeper of verdicts passes all four.

null stub: classify returns None for every spectrum, recommendation() always None, auto-pilot writes no junk filter.
  satisfies: none   fails: 1, 2, 3, 4, 5
hard-coded stub: a table returning the named verdict, and the named log, for the exact spectrum, bandwidth, samplerate and fixture each line hands in.
  satisfies: none   fails: 1, 2, 3, 4, 5 - 1, 4 and 5 demand two different answers from one reader or one manager on one stream, and 2 and 3 demand the verdict move with a parameter (content ceiling, container bandwidth) the table does not read.

survives: 2
