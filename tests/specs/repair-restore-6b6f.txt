slug: repair-restore-6b6f
kind: refactor

brief:
> 05c6f702 test: repair-lane-3f80 (2026-09-11 11:37) deleted both files whole:
> tests/gates/test_excision_diff.py (321 lines) — the tests for the new scripts/excision-diff.py, spec repair-lane-3f80 lines 1-4
> tests/gates/test_pair_excision.py (248 lines) — the pre-existing pair.sh excision tests
> Cause is the fixture, not the implementation. _pair_checkout (tests/gates/test_excision_diff.py:262) and _checkout (tests/gates/test_pair_excision.py:139) copy only scripts/pair.sh into the throwaway checkout. Every excision/repair merge there reaches mechanical_check, runs python3 scripts/excision-diff.py with cwd in that checkout, finds no such file, and dies before step 6 — so no verdict line is printed and dev never fast-forwards, which is exactly both failure shapes.
> Reflow is one line per fixture: copy scripts/excision-diff.py in beside the driver. Assertions and expected values stand.
> Inside the pair: Open a kind: refactor pair on the repair-lane-3f80 block, git restore --source 05c6f702^ both files into the spec tree, brief gauntlet-testsmith with the fixture leak as a known bug, red run expects GREEN 14, merge.
> drop it, land the rest

1. Given a block line naming `tests/<file>::<test>` with a quoted `assertion:`, `python3 scripts/excision-diff.py --spec <block path> --base <commit> --head <commit>`, run with the repository as its working directory, prints to stdout `OK <target>` where that test name is absent from the file at `--head`, `UNSATISFIED <target>` where the name is present with the quoted text inside that test's own body, `OK <target>` again where the name is present but the quoted text sits only in a neighbouring test of the same file.
   kills: searching the whole file for the quoted assertion, which holds a target open on a sibling parametrize case that no block line names and that the writer was never asked to touch.
   bite: HEAD passes this line — the run (`.venv/bin/python -m pytest tests/gates/test_excision_diff.py tests/gates/test_pair_excision.py -q -p no:cacheprovider`) over the files restored from `05c6f702^` prints `4 failed, 9 passed, 2 warnings in 0.96s`, and this line's three cases are not among the four named in the summary; the line is restored, not newly pinned.
   existing: none on dev's committed tip, `grep -rn "excision-diff\|excision_diff" tests/` returns `tests/specs/gauntlet-dirs-f29d.txt:41` and `tests/specs/plans-lane-7c31.txt:5`, both prose inside committed spec blocks; the tests that pinned it were deleted whole by 05c6f702.

2. For a block whose first line is `kind: repair` and which carries one repair line — `N. excise tests/<file>::<test>` followed by indented `rule:`, `assertion:`, `replace:`, `as: tests/<other>::<test_name>` and `kills:` clauses — whose `as:` names a test that does not exist at `--head`, beside one excision line carrying only `rule:` and `assertion:` whose target is gone from its file, that same command prints exactly three verdicts: `OK <repair-target>`, `OK <excision-target>`, `MISSING <as-name>`.
   kills: emitting a landing verdict for every line rather than for the lines that carry `as:`, which prints a fourth verdict and fails a plain excision block for a replacement it never promised.
   bite: HEAD passes this line — the run (`.venv/bin/python -m pytest tests/gates/test_excision_diff.py tests/gates/test_pair_excision.py -q -p no:cacheprovider`) printing `4 failed, 9 passed, 2 warnings in 0.96s`, and `test_only_the_line_carrying_an_as_name_gets_a_landing_verdict` is not among the four named in the summary; the line is restored, not newly pinned.
   existing: none on dev's committed tip, same grep as line 1.

3. For a diff touching three files under `tests/` between the commits passed as `--base` and `--head` — one named by a line's target path, one named only as the file half of an `as:` name, one named by no line — that same command prints exactly one `UNNAMED` verdict, naming the third file.
   kills: collecting only the target paths as the named set, which prints two `UNNAMED` verdicts and refuses a change the block described exactly.
   bite: HEAD passes this line — the run (`.venv/bin/python -m pytest tests/gates/test_excision_diff.py tests/gates/test_pair_excision.py -q -p no:cacheprovider`) printing `4 failed, 9 passed, 2 warnings in 0.96s`, and `test_a_file_named_only_as_the_file_half_of_an_as_name_is_not_reported_unnamed` is not among the four named in the summary; the line is restored, not newly pinned.
   existing: none on dev's committed tip, same grep as line 1.

4. The output of `scripts/pair.sh merge <slug>` for a pair whose committed block at `tests/specs/<slug>.txt` is `kind: excision` carries the checker's verdict lines, for one whose committed block is `kind: new` carries the `TEST CHECK` brief, for one whose committed block is `kind: excision` while the approved copy at `docs/gauntlet/specs/approved/<slug>.txt` reads `kind: new` carries the checker's verdict lines, and for a pair whose committed block is `kind: repair` with no `test: <slug> red` commit on its branch carries the checker's verdict lines.
   kills: reading the kind from the approved copy in the main checkout, which the orchestrator can edit after the reviewer wrote it, rather than from the block committed at `tests/specs/<slug>.txt` on the spec branch — the third input is where the two disagree, and the fourth is where a tests-only pair that skipped the red run reaches the checker rather than the missing-red-commit warning.
   bite: HEAD fails this line for a reason outside the implementation — the run (`.venv/bin/python -m pytest tests/gates/test_excision_diff.py tests/gates/test_pair_excision.py -q -p no:cacheprovider`), whose summary names `FAILED tests/gates/test_excision_diff.py::test_the_merge_output_follows_the_kind_the_committed_block_carries[committed-excision]` and its two sibling cases, at `tests/gates/test_excision_diff.py:321`, because the throwaway checkout the fixture builds carries `scripts/pair.sh` and not `scripts/excision-diff.py`, so `mechanical_check` dies before printing a verdict.
   existing: none on dev's committed tip, same grep as line 1; `tests/gates/test_pair_excision.py` holds the red-run side, and its one test that drives `merge` (`:195`, driving at `:204`) is dropped from this pair by the owner's ruling, so no landing test of that file reaches `merge`.

--- spec-reviewer READY ---
READY
discriminates: sweep on `python3 scripts/excision-diff.py --spec --base --head` stdout (line 1, three inputs, two distinct values; lines 2 and 3 two further values on the same surface) and sweep on `scripts/pair.sh merge` output (line 4, four inputs, two distinct values)

reversal, lines 1-3: I cut these under (b) on a grep of the working tree. I missed that both files are staged and uncommitted - `git cat-file -e 'dev:tests/gates/test_excision_diff.py'` returns "exists on disk, but not in 'dev'" - and that `pair.sh open` cuts the spec tree from dev's committed tip, which carries neither file. There is no test on that tip touching these surfaces, so (b) is clear and the cuts are withdrawn.
repair 2 withdrawn on the same citation: line 4 lands a test that does not exist on dev's tip, so it is a new test, not a delta.
repair 3 withdrawn: the second fixture's only merge-driving test (tests/gates/test_pair_excision.py:195) is dropped from this pair under the brief's own last sentence, "drop it, land the rest", so no brief sentence is left unpinned.

1  KEEP  `python3 scripts/excision-diff.py --spec <block> --base <c> --head <c>` on a block line naming tests/<file>::<test> with a quoted assertion, at three file states -> stdout `OK` (name absent), `UNSATISFIED` (quoted text in that test's own body), `OK` (quoted text only in a neighbouring test); whole-file search for the quoted assertion fails it at the third input, printing UNSATISFIED for a target no block line holds open; bite: HEAD prints the three verdicts (`4 failed, 9 passed` run, this line's cases not among the four), null stub fails at the UNSATISFIED input, printing nothing
2  KEEP  a kind: repair block with one repair line whose `as:` names a test absent at --head, beside one plain excision line -> exactly `OK <repair-target>`, `OK <excision-target>`, `MISSING <as-name>`; a landing verdict per line fails it here with a fourth verdict against the excision line; bite: HEAD prints the three verdicts (`test_only_the_line_carrying_an_as_name_gets_a_landing_verdict` not among the four failures), null stub prints nothing
3  KEEP  a --base..--head diff touching three files under tests/ - a line's target, the file half of an `as:` name, and a file no line names -> exactly one `UNNAMED`, naming the third; collecting only target paths fails it with two UNNAMED verdicts; bite: HEAD prints the one verdict (`test_a_file_named_only_as_the_file_half_of_an_as_name_is_not_reported_unnamed` not among the four failures), null stub prints none
4  KEEP  `scripts/pair.sh merge <slug>` for committed kind: excision -> checker verdict lines, committed kind: new -> TEST CHECK brief, committed kind: excision with the approved copy reading kind: new -> checker verdict lines, committed kind: repair with no red commit -> checker verdict lines; reading the kind from the approved copy fails it at the third input with the TEST CHECK brief; bite: HEAD fails at the third and its sibling cases, `FAILED tests/gates/test_excision_diff.py::test_the_merge_output_follows_the_kind_the_committed_block_carries[committed-excision]` (:321), the throwaway checkout carrying pair.sh and not excision-diff.py

null stub: `scripts/excision-diff.py` present with `parse_block` returning [] and every verdict function returning its zero value, so stdout carries no verdict; `pair.sh` unchanged, so merge prints the TEST CHECK brief for every kind.
  satisfies: none   fails: 1, 2, 3, 4
hard-coded stub: print a canned verdict string per invocation and a canned merge tail per slug, parsing neither the block nor the diff.
  satisfies: none   fails: 1, 2, 3, 4 - each line contrasts inputs differing only in block or file content the stub does not read (line 1's three file states, line 2's as-carrying versus plain line, line 3's three diff paths, line 4's committed versus approved kind).

survives: 4
