slug: bothlanes-7d3c
kind: new
discriminates: differential on /api/health `ready` (same surface, two observations: during the
restart-wait vs at return / with vs without a write in flight); line 3 is a second differential on
AlertStrip's rendered kind set.

behaviors

1. Across a load of a stored preset that restarts the daemon, /api/health reads `ready: false` at
   least once while the load is still running, and `ready: true` at the moment the load returns.
   kills: answering the load from a readiness flag the poll loop has not yet cleared, so the load
     returns before the daemon has even gone down, the page never dims, and the drop lands after
     the user has been told the change is done.
   bite: 807 samples, 0 read `ready: false`, 0 read `reachable: false`, `ready` reads True (and
     `reachable` True) at the moment `presetlane.load` returns, load wall clock 0.015 s
     (`PYTHONPATH=/srv/hqptuner:tests:tests/support:tests/api .venv/bin/pytest -c pyproject.toml
     --rootdir=. -p conftest -s -q <scratchpad>/test_probe_bothlanes.py::test_probe_line_1`, a
     scratchpad probe reusing the `across_a_restart` and `restarting_daemon` fixtures from
     tests/api/test_health_ready.py, sampling `health(manager)` on a concurrent task at
     `await asyncio.sleep(0)` for the whole load call). Re-checked once: 819 samples / 0 false /
     0 false / True at return / 0.014 s. `restarting_daemon["dropped"]` was 1 in both runs, so the
     restart did fire, but only after the load had returned.
   existing: none. `grep -rn '"ready"' tests/` returns tests/api/test_health_ready.py:64-70,
     :81, :174 and tests/e2e/support/stack.py:162; :174 asserts `ready` at the moment the load
     returns and nothing during it, :64-70 is two reads across a connect with no write in flight,
     :81 is the credential-less read, and stack.py:162 is a readiness gate, not an assertion.

2. A failing configuration-lane read makes /api/health read `ready: false` while a preset load is
   still waiting out the daemon restart, and leaves it reading `ready: true` when no such write is
   in flight.
   kills: two wrong implementations at once. Waiting only for the control connection to come back,
     so the app reports itself ready with the configuration lane still down and the next write
     fails against a lane nobody said was missing. And consulting the configuration lane at all
     times, so an ordinary form-read blip during a live matrix profile switch dims the whole page
     and takes the pill off green on a write that never restarted anything.
   bite: both halves read the same value at HEAD, which is what makes the pair discriminate.
     Load-in-flight half: failing state `ready: True`, `reachable: True`, `readings.config_error`
     "Server error '503 Service Unavailable' for url 'http://127.0.0.1:<port>/config'"; answering
     state `ready: True`, `reachable: True`, `config_error: None` (same command as line 1,
     `::test_probe_line_2`; the `across_a_restart` manager after a completed
     `presetlane.load(mgr, "Stored")` with `dropped == 1` and `reachable is True`, then
     `http_daemon["_fail_paths"] = ["/config"]` and a full poll, then `_fail_paths = []` and
     another full poll). No-write-in-flight half: `ready: True`, `reachable: True`, same
     `config_error` 503 string (same command, `::test_probe_line_3`;
     `start_manager(http_daemon["_port"])` settled, `_fail_paths = ["/config"]`, one full poll).
   existing: none. `grep -rn config_error tests/` returns one hit,
     tests/apply/test_manager_connect_load.py:45, which asserts `config_error is not None` after a
     connect with a dead 8088 lane and asserts nothing about `ready`; the two neighbouring tests at
     :32-40 pin `file_config` and `reachable` for the same input.

3. When a preset pick has failed, the alert strip renders one row carrying the failure; with no
   failure recorded it renders none.
   kills: reporting the failure only through the pending bar's result line, which the LIVE page
     never puts on screen, so a pick that failed in LIVE tells the user nothing at all.
   bite: surface new. Null stub hqptuner/static/store/alerts/presetpick.js exporting
     `presetPickAlert`, a computed returning `{kind, sev, text}` or null, the shape
     `credentialsAlert` and `roonIdleAlert` already return and that AlertStrip spreads; the stub
     returns null always and fails the first half. No module in the checkout carries a preset-pick
     failure: hqptuner/static/store/alerts/ holds only credentials.js, junkadvice.js, roonidle.js
     and shaperfit.js, and AlertStrip imports exactly those four plus store/health.js. Rendered
     output of AlertStrip today with no alert conditions set: "" — zero rows, zero characters
     (`node --import <scratchpad>/resolve.mjs <scratchpad>/probe_strip2.mjs`). Re-checked in the
     same run: "" before any signal write, one row after one clipping status frame, "" again after
     clearing.
   existing: none. `grep -rn "alert-strip\|AlertStrip" tests/js` returns
     tests/js/components/alertstrip.test.js, tests/js/components/shaperfit-strip.test.js,
     tests/js/components/junkadvice.test.js and tests/js/store/roonidle.test.js; all four drive the
     strip from engine health, shaper fit, junk advice and Roon idle, and none mentions a preset,
     a pick or a delete.

folded delta (not a numbered line)

tests/js/components/header.test.js:238, :242 and :246 pin the "(pending apply)" marker through the
`pendingMarked` helper at :117-118, asserting it present for a previewed preset, present for a
previewed "(no preset)", and absent for an active preset alone. The marker is being removed, so the
first two invert to absent and the third stands as it is; the helper is unchanged.

public entry points

  # hqptuner/core/manager.py - ConnectionManager
  ready: bool                            # was a plain attribute; the name and what it means to a
                                         # caller are unchanged
  async def restore(self, archive: bytes, *, scope: str = "system") -> None   # new
  async def await_ready(self) -> bool          # unchanged signature
  async def await_http_ready(self) -> bool     # unchanged, stays

  # hqptuner/api/routes/status.py
  def health(manager: Mgr) -> dict[str, Any]   # the "ready" key is unchanged

  # hqptuner/presets/presetlane.py  (unchanged signatures)
  async def load(mgr: ConnectionManager, name: str) -> dict[str, Any]
  async def switch(mgr: ConnectionManager, name: str) -> dict[str, Any]

  # hqptuner/lanes/http/restore.py  (unchanged signature)
  async def apply(mgr: ConnectionManager, edits: dict[str, str], *, switched: bool = False) -> dict[str, Any]

  // hqptuner/static/store/alerts/presetpick.js          (new module)
  export const presetPickFailure = signal(null)   // the last failed pick or delete, or null
  export const presetPickAlert = computed(...)    // {kind, sev, text} or null

  // hqptuner/static/components/AlertStrip.js
  export function AlertStrip()                    // unchanged signature; one more row in the list

  // hqptuner/static/components/Header.js
  export function Header()                        // unchanged signature

wire facts

- 4321 is unauthenticated and never restarts the daemon (docs/architecture.md:16).
- Persistent writes ride POST /restore, on which hqplayerd self-restarts in ~5.6 s
  (docs/architecture.md:17). Every path that restarts the daemon goes through that POST.
- Read-only use and all live settings work with no credentials (docs/architecture.md:37), so an
  install without them has no 8088 lane to wait for, ever.
- result="OK" is not proof; readback verifies (docs/architecture.md:26).
- The default poll interval is 2.0 s and the alarm threshold 15.0 s (hqptuner/config.py:49-50).

fixtures

- across_a_restart (tests/api/test_health_ready.py:131) and restarting_daemon
  (tests/api/test_health_ready.py:91) - a manager whose 8088 restore actually drops the control
  connection, with a `dropped` counter; lines 1 and 2's in-flight half.
- start_manager (tests/conftest.py:244) - 4321 fake plus an 8088 lane; line 2's settled half.
- settled (tests/conftest.py:234) - full connect plus one completed poll.
- http_daemon (tests/support/fixtures_daemons.py:158) - the 8088 fake, with `_fail_paths` to make
  one path answer 503; both halves of line 2.
- alertKinds (tests/js/components/alertstrip.test.js:37) and the CREDENTIAL_CASES shape at :79-87 -
  the strip's rendered `data-alert` identifiers; line 3.
- head() in tests/js/components/header.test.js:71-73 - per-call health and config overrides; the
  folded delta.

changelog (lands verbatim, under [Unreleased] -> ### Fixed)

- **Preset actions report failures under the signal path.** The header's preset row no longer
  prints status text or a pending marker, and a pick or delete that fails now shows a warning on
  the alert strip, which is on screen in LIVE as well.

The readiness fix itself gets no entry: the line already under [Unreleased], "The app reports
itself connected only once both daemon lanes are up", describes exactly this behavior and shipped
in the same unreleased window. This work is what makes it true, not a second change.

--- spec-reviewer READY ---

READY
discriminates: differential on /api/health `ready` (same surface, two observations: during the restart-wait vs at return / with vs without a write in flight); line 3 is a second differential on AlertStrip's rendered kind set.

1  DELTA tests/api/test_health_ready.py:169: that test already drives this exact input (save_preset + presetlane.load across_a_restart, _died) and already asserts the second half at :175; fold the in-flight observation into it — a concurrent sampler on the existing `_at_the_first_pass_where`/`asyncio.sleep(0)` idiom at :43-55, one assertion on the pair `(saw_not_ready_during, ready_at_return) == (True, True)`. No new file, no new fixture: `restarting_daemon` and `across_a_restart` are at :91 and :131.
2  KEEP  config lane returning 503 on /config while a stored-preset load is waiting out the restart -> health `ready` False; same 503 with no write in flight -> `ready` True; "wait only for the 4321 connection" fails it at the in-flight input (reports ready with 8088 still down) and "consult the config lane always" fails it at the settled input (ready False on a blip that restarted nothing); bite: measured `ready: True` / `config_error` 503 at the settled input, and line 1's own measurement at HEAD (0 of 807, 0 of 819 samples read `ready: false` anywhere inside the load) is the HEAD value for the in-flight input.
3  DELTA tests/js/components/alertstrip.test.js:79-87: same shape as `CREDENTIAL_CASES` — add the preset-pick condition as a parametrized pair over `alertKinds` (:37), failure -> `["<kind>"]`, no failure -> `[]`, driven off the exported signal the same way `idleStrip` (:70) drives `health`. The assertion is on the row's `data-alert` identifier, never on the row's sentence (rule 9); a line asserting "a row carrying the failure" without naming the kind string would be CUT under (c)/(h).

null stub: no preset-pick alert module, no readiness change — health.ready never leaves True and AlertStrip renders "" — fails line 1's in-flight half, line 2's in-flight half, and line 3's failure half; satisfies nothing in the block.
hard-coded stub: returning `ready: False` for the literal restart-load input and `["presetpick"]` for the literal failure input still has to know a load is in flight and that a pick failed, which is the feature; it cannot satisfy line 2's second half and line 3's second half at the same time without tracking both conditions.
survives: 1

notes:
- Framing struck, reviewed as absent: "folded delta (not a numbered line)" is the author ruling on how the block is counted. Deltas do not count toward the cap by rule, not by the author's say-so. One tell only; no rejection.
- The header.test.js fold (:238, :242, :246 via `pendingMarked` at :117-118) is a legitimate delta and reads correctly against the file: two assertions invert to absent, the third stands. It carries no `kills:`/`bite:` and needs none.
- Unread by me (hqptuner/ denied), for the owner: that hqptuner/static/store/alerts/ holds only credentials.js, junkadvice.js, roonidle.js and shaperfit.js and that AlertStrip imports exactly those four plus store/health.js — line 3's `existing: none` claim rests on it. The tests side is consistent: alertstrip.test.js pins clipping and credentials only.
- Line 2's in-flight bite was measured at a substitute input (after a completed load, not during one). Accepted only because line 1's HEAD measurement covers the in-flight window directly and reads True throughout; if line 1 is dropped, line 2's bite becomes unfilled.
