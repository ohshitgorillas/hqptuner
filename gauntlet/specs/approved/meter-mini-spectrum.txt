slug: meter-mini-spectrum
kind: new
brief:
> Find the approved plan file below. Take the slug through stage 2 and present the spec block when READY.
> slug: `meter-mini-spectrum`
> plan: `gauntlet/plans/approved/meter-mini-spectrum.txt`

1. `hqptuner.engine.metering.band_levels(power, bandwidth)` over a 1025-bin frame whose power sits in the bins nearest 100 Hz returns its largest of the three values at index 0; over the same frame shaped at 1000 Hz at index 1; at 10000 Hz at index 2, all three at bandwidth 22050.0.
   kills: adds every bin into all three values, so a 100 Hz tone and a 10 kHz tone read the same height on all three bars.
   bite: null stub fails at the 100 Hz frame (surface new) — a stub returning (0.0, 0.0, 0.0) has no single largest value at any index.
   existing: none, `grep -rn 'band_levels' tests --include=*.py` returns only `tests/engine/test_junkadvisor.py:120`, a local helper building junk-advisor levels from band tuples, against `classify`.

2. `hqptuner.engine.metering.band_levels(power, bandwidth)` over a 1025-bin frame carrying a 2000 Hz tone returns its largest value at index 1 at bandwidth 22050.0 and at index 1 at bandwidth 96000.0.
   kills: cuts the three spans at fixed fractions of the bin count, so the same 2 kHz tone reads mid on a 44.1 kHz stream and low on a 192 kHz one.
   bite: null stub fails at the 2000 Hz frame at bandwidth 96000.0 (surface new) — a stub returning (0.0, 0.0, 0.0) has no single largest value at index 1.
   existing: none, `grep -rn 'band_levels' tests --include=*.py` returns only the junk-advisor helper cited in line 1.

3. A `hqptuner.engine.metering.BandRing` given (-20.0, -60.0, -60.0) and then (-60.0, -60.0, -60.0) 0.1 s of frame time later returns a larger first value from `mean()` than one given (-40.0, -60.0, -60.0) and then (-60.0, -60.0, -60.0) at that same 0.1 s spacing.
   kills: reports the newest triple alone, so both readings sit at the quiet frame's height and the bars jump from frame to frame instead of riding the second they cover.
   bite: null stub fails at the (-20.0, -60.0, -60.0) pair (surface new) — a stub whose `mean()` returns None gives no value to compare against the other reading.
   existing: none, `grep -rn 'BandRing' tests --include=*.py` returns no match.

4. `GET /api/status` against an app reading a 4322 stream of 1025-bin frames at bandwidth 22050.0 whose power sits in the bins nearest 100 Hz returns a `data.bands` element 0 greater than the `data.bands` element 0 the same app returns while reading a stream of the same frames shaped at 10000 Hz.
   kills: serves a constant triple without reading the stream, so the bars show the same three heights whatever plays.
   bite: null stub fails at the 100 Hz stream (surface new) — a stub serving `data.bands` as null has no element 0 to compare.
   existing: none, `grep -rn 'bands' tests/api --include=*.py` returns no match.

--- reviewer ---
READY
discriminates: sweep on hqptuner.engine.metering.band_levels - three tone frequencies to three distinct indices at one bandwidth (line 1), one tone across two bandwidths (line 2); differential on BandRing.mean (line 3) and on GET /api/status data.bands (line 4)

1  carried ADMITTED
2  carried ADMITTED
3  carried ADMITTED
4  carried ADMITTED

null stub: module present, band_levels returns (0.0, 0.0, 0.0), BandRing.mean returns None, /api/status serves data.bands as null.
  satisfies: none   fails: 1, 2, 3, 4
hard-coded stub: return a fixed triple per exact input frame and bandwidth, doing no bin-to-band work.
  satisfies: none   fails: 1, 2, 3, 4 - lines 1 and 2 together force a frequency-to-band mapping that scales with bandwidth (line 1 holds bandwidth at 22050.0 across three frames, line 2 holds one frame across two bandwidths), and a stub keyed on either alone fails the other.

survives: 4

cut, not written to the approved file: the `entry points:`, `wire facts:` and `fixtures:` sections added this round. They are not part of a spec block (docs/approved-specs.md, "What the reviewer writes": structure line, brief, behavior lines), they pin nothing, and the approved file is the surviving contract. Two further reasons they do not travel:
- `wire facts:` states a conclusion about implementation I am barred from reading - "GET /api/status refuses with 503 until the first Status frame has landed" (hqptuner/api/routes/status.py:88). Unverified by me; it fills no field of line 4, whose bite is a null-stub bite and stands without it.
- `fixtures:` misstates a readable fact. It says neither tests/conftest.py's harness section nor tests/support/fixtures_daemons.py carries a 4322 stream today; tests/support/fixtures_daemons.py:6 reads "fakes from `fake_http`, and the fake 4322 metering streams", tests/conftest.py:385 reads "metering reader harness (fake 4322 stream lives in fixtures_daemons)", and tests/e2e/support/stack.py:4 names `fake_metering` (4322). A writer told to build the fake from wire facts would duplicate an existing harness. This changes no verdict: line 4's `existing:` is about a test pinning the outcome, and `grep -rn 'bands' tests/api --include=*.py` still returns no match.

greps re-run this round and confirmed: `grep -rn 'band_levels' tests --include=*.py` returns only tests/engine/test_junkadvisor.py:120 and its three call sites, a local helper against `classify`; `grep -rn 'BandRing' tests --include=*.py` returns no match; `grep -rn 'bands' tests/api --include=*.py` returns no match.
