slug: desc-hovertips
kind: new

brief:
> Issue: Several locations in the UI maintain explanatory text in spite of the "Settings descriptions" and "Option descriptions" options (System tab > HQPTuner card) being disabled.
> Task: Convert the explanation text to hover tips as per the option's intent.
> Locations:
> - HQPTuner card itself; Setting descriptions, Option descriptions, Apodizing indicator, and Dyslexic font
> - Matrix tab, Crossfeed card: Bauer vs Structural explanation (hover tip goes to [Bauer|Structural] switch)
> - Matrix tab, Crossfeed card, Crossfeed compensation sub-section (hover tip goes to the plot and slider) note: only the paragraph beginning "Guide:" is converted to hover tips; leave the rest of the text
> - Output tab, both Backend cards: "Stops the engine. All live settings except matrix profiles survive."
> I have personally identified all sites in the code requiring this treatment.
> If a location with text is omitted from this list, that is intentional.
> Do not suggest additional text locations to clean up or expand the spec beyond what has been provided.
> All quoted lines are defects of "Setting descriptions", "Option descriptions" currently works as intended.
> Do not alter the latter, only the former.

1. Per row, the pair (decoded texts of the site's inline explanatory elements, decoded `title` of the tip target) reads with Setting descriptions on (the on-state texts, absent), then with it off (the on-state texts minus the converted element's, the converted element's text). Rows as site / converted element / tip target: HQPTuner card `section[data-card="hqptuner"]` with keepOptionDescriptions on, once per row `data-k` `showDescriptions`, `keepOptionDescriptions`, `apodLight`, `dyslexic` / that row's `.field-note` / that row's root `.field`; Crossfeed card in Bauer view with crossfeed enabled, elements the `.field-note` inside `.xfs-top` / that note / root `span.segment` of the view switch whose buttons carry `data-v` `bauer`, `structural`; compensation strip with crossfeed enabled, elements its `.xfc-scale` spans / the second `.xfc-scale` in document order at the on state / `.slidernum` root, once unlocked (symmetric stereo pair in pipelines 1+2), once locked (one pipeline row, range input rendered `disabled`); same strip elements / same converted span / `.xfc-mini` root of the mini plot, unlocked; Field `alsa_device`, then Field `net_device`, autosave on, elements its `.field-rescan-cost` / that caption / `data-testid="rescan"`; Field `alsa_device` with autosave off, elements its `.field-rescan-cost` / none / `data-testid="rescan"`, where the pair reads (no texts, absent) in both states.
   kills: converting the first `.xfc-scale` span, the percentage scale line, in place of the second, which reads (second span's text, first span's text) off where the row expects (first span's text, second span's text), leaving the paragraph the brief converts; a tip set in both states, which reads a present title on; a tip carrying a neighbour's text, such as the Option descriptions row wearing the Setting descriptions note; a rescan tip that ignores autosave, which reads a present title at autosave off, where the sentence it carries is false; the shipped code, which reads (on-state texts, absent) in both states on every row.
   bite: HEAD reads, at showDescriptions true then false: inline counts 4/4 in the HQPTuner card, 1/1 in `.xfs-top`, 2/2 `.xfc-scale` in the strip, 1/1 `.field-rescan-cost` on each of `alsa_device`, `net_device` with autosave true; no `title` on any target in either state (four System row roots, both `span.segment` in `.xfs-top`, `.slidernum` unlocked, `.slidernum` locked with the locked state reached, `.xfc-mini`, `data-testid="rescan"` on both device fields at autosave true, `data-testid="rescan"` on `alsa_device` at autosave false); so every off-state pair fails at both halves on every row except the autosave-off rescan row, which reads (no texts, absent) at HEAD. Re-counted once with a separate regex, same counts; re-checked once printing each raw opening tag, the same reader finding titles on the accent swatches, `.xfs-lens`, `.xfc-tilt` (`node --import ./tests/js/support/vendor-resolve.js --test <scratchpad>/desc-hovertips-measure.test.mjs`, gauntlet-accountant, probe deleted after run).
   existing: tests/js/components/chrome/field-rescan-caption.test.js::test_a_rescan_field_carries_the_engine_stop_caption_when_autosave_is_on, ::test_a_rescan_field_carries_no_engine_stop_caption_when_autosave_is_off pin the caption's presence by autosave at notes on (harness `desc = true`, field-harness.js:145), neither reads the button's title; tests/js/components/chrome/fielddesc.test.js::test_a_hidden_inline_note_moves_the_tooltip_to_the_hover pins a Field root's title from settings metadata at `desc: false`, which the HQPTuner card rows do not reach, since those rows are hand-built `div.field` elements with literal notes, not `Field` components (hqptuner/static/components/tabs/SystemTab.js:101-166); tests/js/components/xfeed/xfeedcomp.test.js::test_hidden_notes_render_no_explanation, ::test_shown_notes_explain_what_the_crossfeed_does pin the `xfc-note` element (`div.field-note.xfc-note`, gated at hqptuner/static/components/xfeed/Comp.js:261), a different element from the `.xfc-scale` spans (Comp.js:318-323, rendered unconditionally, scale line first, Guide paragraph second); `grep -rn -e xfc-scale -e field-rescan-cost -e 'hqptuner"' -e xfc-mini tests/js` returns field-rescan-caption.test.js:34, crossfeed.test.js:211 only; `grep -rn -e titleOf -e 'title=' tests/js/components/tabs/systemtab.test.js tests/js/components/xfeed tests/js/components/controls tests/js/components/chrome/field-rescan-caption.test.js tests/js/components/chrome/field.test.js tests/js/components/tabs/outputtab.test.js` returns combobox-plainquiet.test.js:37, :270, :274 only.

public entry points, wire facts, fixtures, changelog line: none supplied with the block as reviewed.
--- spec-reviewer READY ---
READY
discriminates: differential on showDescriptions true/false per row, a (texts, title) state value compared across the two renders, with distinct expected texts across rows
round 1 findings: repair 1 applied (the converted span is named by position, the second `.xfc-scale` at the on state); repair 2 applied (old lines 1 and 3 folded in as rows); repair 3 withdrawn on citation SystemTab.js:101-166 (hand-built rows, not `Field`, so fielddesc.test.js:52 does not reach them); repair 4 withdrawn on citation Comp.js:261 vs :318-323 (`xfc-note` is a different element from the `.xfc-scale` spans). Round 1 verdicts on old lines 1, 2, 3 (CUT, CUT, DELTA) are superseded by the fold, not reversed.

1  KEEP  each named site/target at showDescriptions true then false -> (on-state texts, absent) then (on-state texts minus the converted element's, its text), with the autosave-off rescan row (no texts, absent) in both states; converting the first `.xfc-scale` (scale line) fails it at the strip rows, a both-states tip fails every on half, a neighbour's text fails the System rows, an autosave-blind rescan tip fails at `alsa_device` autosave off; bite: HEAD reads 4/4, 1/1, 2/2, 1/1 inline and no title on any target in either state, so every off half fails except the autosave-off row

null stub: HEAD as measured: every inline note rendered in both states, no title on any target.
  satisfies: none   fails: 1 (every off-state pair except the autosave-off rescan row)
hard-coded stub: at showDescriptions false, hides each site's notes whole (both `.xfc-scale` spans included), and stamps a fixed literal title on every target including the rescan button whatever the autosave state.
  satisfies: none   fails: 1 (strip rows read no texts where the scale line should stay; the autosave-off rescan row reads a title)

survives: 1
