slug: junkgate-5d27

motion: strike

brief:
> The slug deletes the readiness gate, the track_serial check and the track-change rebuild from hqptuner/engine/metering.py, removes the remaining coverage exemptions in scripts/gates/check_coverage_floor.py, and amends docs/architecture.md:98 because the spur hold's only track boundary becomes visibility release
> EXCISE THE TESTS THAT NO LONGER PIN VALID BEHAVIOR.

1. strike tests/engine/test_junk_persistence.py::test_window_min_is_none_before_the_window_is_earned
   removed: The slug deletes the readiness gate, the track_serial check and the track-change rebuild from hqptuner/engine/metering.py, removes the remaining coverage exemptions in scripts/gates/check_coverage_floor.py, and amends docs/architecture.md:98 because the spur hold's only track boundary becomes visibility release
   assertion: assert aggregate.window_min_db() is None

--- reviewer ---
READY
discriminates: N/A - motion: strike pins nothing; the discrimination check does not apply

1  ADMITTED  strike tests/engine/test_junk_persistence.py::test_window_min_is_none_before_the_window_is_earned; `removed:` cites brief sentence 1 verbatim, which names the readiness gate as dropped; the quoted assertion is byte-identical to line 42 of that test and pins that gate (WINDOW_BLOCKS - 1 blocks fed, value withheld); exactly one citation field, `removed:` and no `rule:`.

null stub: feature absent - nothing rendered, nothing persisted, every new function returns its zero value.
  satisfies: none   fails: none   (a strike block pins no outcome for either stub to meet)
hard-coded stub: return the named values at the named inputs and do no work.
  satisfies: none   fails: none   (a strike block pins no outcome for either stub to meet)
survives: 1

notes for the owner, neither a bar:
- The production work in the brief - the readiness gate, the track_serial check, the track-change rebuild, the coverage exemptions, docs/architecture.md:98 - is pinned by no line here and cannot be: a strike block pins nothing. The main agent states it rides with gauntlet/plans/approved/junkinstant.txt and that slug's own `kind: new` block. Both are outside what I may read, so that split is the owner's to confirm; if that block does not carry it, nothing does.
- Two round-1 grounds of mine were wrong and are withdrawn on the record: `removed:` is a real citation form (plugin docs/testing.md:81-89, which I had not read - I stopped at the repo's older copy), and the block-level (m) bar rested on a plan file I cannot open. Round 2's finding stands as accepted: this test guards an invariant, so no `rule:` fits it, and `removed:` is the only route that could carry it.
- What I could check, I checked in tests/engine/test_junk_persistence.py alone. Whether the readiness gate is in fact going away is the owner's word in the brief, taken as the fact the form makes it.
