slug: junkinstant

kind: new

brief:
> The slug deletes the readiness gate, the track_serial check and the track-change rebuild from hqptuner/engine/metering.py, removes the remaining coverage exemptions in scripts/gates/check_coverage_floor.py, and amends docs/architecture.md:98 because the spur hold's only track boundary becomes visibility release

1. The windowed minimum spectrum of a `SpectralAggregate(bins=8, bandwidth=48000.0)` reports at bin 2 the level the frame fed carried there, at partial coverage as at a full block: -53.5 dB where the fed frame carries `10 ** (-53.5 / 10)` at bin 2, -71.2 dB where it carries `10 ** (-71.2 / 10)`, each at `covered_seconds=1.0` and at `covered_seconds=BLOCK_SECONDS`.
   kills: an implementation that withholds the minimum until six full blocks have been folded, so a tone the rules can read off the opening seconds of a stream is not read for half a minute.
   bite: HEAD returns None at all four inputs. Examiner, throwaway script under the session scratch dir, deleted in the same run (`.venv/bin/python <scratch>/probe.py`), the fed frame being eight linear powers at `10 ** (-120.0 / 10)` with index 2 carrying the level named, one `add` then one `window_min_db()` read: `10 ** (-53.5 / 10)` at `covered_seconds=1.0` gives None and at `covered_seconds=BLOCK_SECONDS` gives None; `10 ** (-71.2 / 10)` at `covered_seconds=1.0` gives None and at `covered_seconds=BLOCK_SECONDS` gives None.
   existing: none, `grep -rn "window_min_db" tests/ --include=*.py` gives five lines, all in `tests/engine/test_junk_persistence.py`: the module docstring at line 5 and four assertions at lines 42, 56, 60 and 86, every one of them fed whole blocks at `covered_seconds=BLOCK_SECONDS` and read at or past `WINDOW_BLOCKS`, so none of them reads the aggregate at partial coverage. Neither level named here is a level that file feeds: its frames carry `10 ** (-20.0 / 10)` at bin 2 and `10 ** (-90.0 / 10)` or `10 ** (-20.0 / 10)` at bin 5 (lines 22-35). The one test that pinned the readiness gate, `test_window_min_is_none_before_the_window_is_earned`, left the tree in commit 6eb01005 together with the approved strike block `gauntlet/specs/approved/junkgate-5d27.txt` that targeted it; both paths are gone at HEAD.

--- reviewer ---
READY
discriminates: sweep on `SpectralAggregate.window_min_db()[2]` — two fed levels, each read at `covered_seconds=1.0` and at `covered_seconds=BLOCK_SECONDS`

1  ADMITTED  one `add` of an eight-bin frame at the `10 ** (-120.0 / 10)` floor with bin 2 carrying `10 ** (-53.5 / 10)` or `10 ** (-71.2 / 10)`, at `covered_seconds=1.0` and at `covered_seconds=BLOCK_SECONDS` -> `window_min_db()[2]` is -53.5 dB and -71.2 dB respectively at both coverages; an implementation that withholds the minimum until six full blocks are folded fails it at `covered_seconds=1.0` after one `add`, returning None where the line requires -53.5; bite: HEAD returns None at all four inputs (`.venv/bin/python <scratch>/probe.py`, examiner)

null stub: `window_min_db()` returns None and `add` records nothing.
  satisfies: none   fails: 1
hard-coded stub: `window_min_db()` returns a fixed spectrum for the exact frames named, ignoring `covered_seconds` and the window.
  satisfies: none   fails: 1

survives: 1
