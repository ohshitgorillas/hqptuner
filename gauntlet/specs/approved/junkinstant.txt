slug: junkinstant

kind: new

brief:
> The slug deletes the readiness gate, the track_serial check and the track-change rebuild from hqptuner/engine/metering.py, removes the remaining coverage exemptions in scripts/gates/check_coverage_floor.py, and amends docs/architecture.md:98 because the spur hold's only track boundary becomes visibility release

1. The windowed minimum spectrum of a `SpectralAggregate(bins=8, bandwidth=48000.0)` reports at bin 2 the level the frame fed carried there, at partial coverage as at a full block: -20.0 dB where the fed frame carries `10 ** (-20.0 / 10)` at bin 2, -90.0 dB where it carries `10 ** (-90.0 / 10)`, each at `covered_seconds=1.0` and at `covered_seconds=BLOCK_SECONDS`.
   kills: an implementation that withholds the minimum until six full blocks have been folded, so a tone the rules can read off the opening seconds of a stream is not read for half a minute.
   bite: HEAD returns None at all four inputs. Examiner, throwaway scripts under the session scratch dir, deleted in the same run (`.venv/bin/python <scratch>/agg_b1.py` for the -20.0 pair, `.venv/bin/python <scratch>/agg_measure.py` for the -90.0 pair), the fed frame being eight linear powers at `10 ** (-120.0 / 10)` with index 2 carrying the level named: `10 ** (-20.0 / 10)` at 1.0 s gives None and at BLOCK_SECONDS gives None; `10 ** (-90.0 / 10)` at 1.0 s gives None and at BLOCK_SECONDS gives None. `window_min_db()` requires `len(self._blocks) >= WINDOW_BLOCKS`, and one `add` pushes at most one block.
   existing: none, `grep -rn "window_min_db" tests/` gives five lines, all in one file: `tests/engine/test_junk_persistence.py::test_window_min_is_none_before_the_window_is_earned`, struck by the approved block at `gauntlet/specs/approved/junkgate-5d27.txt`, and four cases that feed whole blocks.

2. A metering reader holding a persistent-tone verdict off a 4322 stream at one frame geometry, whose track context changes `track_serial` partway through and changes nothing else: the `filter` the verdict names on the first read after the change is the one it named before it, `30k` for a tone at 29.9 kHz and `40k` for a tone at 48.2 kHz, both in a 192 kHz container.
   kills: an implementation that treats a track boundary as a reason to drop evidence, so a tone the rules can still see on the new track loses its advice until a fresh window is earned.
   bite: HEAD returns None on the first read after the change at both inputs, against a live verdict immediately before it. Examiner, throwaway script deleted in the same run (`.venv/bin/python <scratch>/measure2.py`), one asyncio TCP server on loopback standing in for the 4322 stream, samplerate 192000, bandwidth 96000, bins 200, channels 2, sdm False, junk_filter None, 12 ingested frames of 1.0 s apiece before the flip and 4 after it, `track_serial` going from "track-1" to "track-2" with every other context field held: tone at 29.9 kHz gives `{'filter': '30k', 'families': ['poly-sinc-gauss-hires', 'poly-sinc-ext2-hires'], 'ceiling_khz': 29.9}` before the flip and None on the first read after it; tone at 48.2 kHz gives `{'filter': '40k', 'families': ['poly-sinc-gauss-hires', 'poly-sinc-ext2-hires'], 'ceiling_khz': 48.2}` before the flip and None on the first read after it.
   existing: none, `grep -rn "MeteringReader" tests/` printed no lines, rc=1.

--- reviewer ---
READY
discriminates: sweep on SpectralAggregate.window_min_db (two fed levels, -20.0 and -90.0, at two coverage values) and on MeteringReader.verdict()["filter"] (30k and 40k at two tone frequencies)

return read finding by finding, round 1:
- "hard-coded stub satisfies line 1, only one fed level appears": repaired, line 1 now feeds -20.0 and -90.0 at bin 2 and expects each back; a constant return fails one of the two.
- "(i) three outcomes in one line": repaired, one condition (bin 2 reports the level fed there) over four parametrized cases.
- "(b) line 1 contradicts tests/engine/test_junk_persistence.py:38": citation supplied and checked. gauntlet/specs/approved/junkgate-5d27.txt is an approved motion: strike block whose one line targets test_window_min_is_none_before_the_window_is_earned with the assertion quoted from line 42. Finding withdrawn.
- "line 2 is an equality any constant satisfies": repaired, the outcome is now a named field value, 30k and 40k, and the null stub's None fails both.
- "(m) the spur-hold clause pins nothing": answered by the repaired line 2, which pins a held tone's advice standing on the new track. Finding withdrawn.

1  ADMITTED  SpectralAggregate(8, 48000.0) fed one frame whose bin 2 carries 10 ** (-20.0 / 10), then one carrying 10 ** (-90.0 / 10), each at covered_seconds=1.0 and at covered_seconds=BLOCK_SECONDS -> window_min_db()[2] reads -20.0 dB and -90.0 dB respectively; an implementation withholding the minimum until WINDOW_BLOCKS blocks are folded fails it at covered_seconds=1.0, where it reads None instead of the fed level; bite: HEAD returns None at all four inputs (measured, two scratch scripts named with their commands). The one existing test that asserts the contrary, tests/engine/test_junk_persistence.py:38, is struck by the approved block gauntlet/specs/approved/junkgate-5d27.txt; the remaining four cases feed whole blocks, so no fold target exists.
2  ADMITTED  a metering reader off a 4322 loopback stream at 192000/96000/200 bins/2 ch/sdm False, holding a persistent-tone verdict, whose track context flips track_serial from "track-1" to "track-2" with every other field held -> verdict()["filter"] on the first read after the flip reads 30k for a 29.9 kHz tone and 40k for a 48.2 kHz tone; an implementation that drops evidence at a track boundary fails it at the first read after the flip, where it reads None; bite: HEAD returns None on the first read after the flip at both tone frequencies against the live 30k and 40k verdicts immediately before it (measured, scratch script named with its command). existing: none, grep -rn "MeteringReader" tests/ rc=1, checked; tests/engine/test_junkadvisor.py::test_the_lower_tone_sets_the_corner pins the same two corners off a bare spectrum through classify(), never a reader across a track change, so the surface here is untested and the line is not a case to fold into it.

null stub: metering module present, exports named, window_min_db() and verdict() return None at every input, nothing folded and nothing classified.
  satisfies: none   fails: 1, 2
hard-coded stub: return a fixed level list with -20.0 at bin 2 from window_min_db() once any frame has been added, and a fixed 30k verdict dict from verdict() on every read, doing no spectral work.
  satisfies: none   fails: 1 (the -90.0 pair), 2 (the 48.2 kHz case)
survives: 2
